
      <h2 class="dog-title"><strong>Dogs</strong></h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th><p>犬種</p></th>
            <th><p>ワンちゃんについて</p></th>
            <th><p>ユーザーネーム</p></th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% dogs.each do |dog| %>
          <tr>
            <td>
              <%= link_to dog_path(dog.id) do %>
                <%= attachment_image_tag dog, :image, size: "300x300" %>
              <% end %>
            </td>
            <td>
              <%= link_to dog_path(dog.id) do %>
                <p><%= dog.name %></p>
              <% end %>
            </td>
            <td>
            <p><%= dog.caption %></p>
            </td>
            <td>
            <p><%= dog.user.name %></p>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>